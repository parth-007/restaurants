const customSortOptions: string[] = [
    'Name', 'Locality', 'ETA', 'Ratings', 'AvgBillFor2'
]

const sortTypes: string[] = [
    'Ascending', 'Descending'
];

const tags: string[] = [
    'Healthy', 'Quick', 'South-Indian', 'Italian', 'Mexican', 'Japanese', 'Chinese', 'Punjabi'
];

const noDataFound = 'No data found!';

const restaurantsPerPage = 5;

const footerNote = 'All rights reserved!';

// Filters
const tagFilters = 'Pick Filters:';
const selectPrice = 'Select Min and Max Avg price for 2 people:';
const filterRestaurants = 'Filter Restaurants!';

// Search Header
const searchString = 'Search';

// Restaurant Card
const avgBillFor2 = 'Avg. Bill for Two:';
const tagsString = 'Tags:';
const estTimeString = 'Est.Time:';
const ratingsString = 'Ratings:';
const addressString = 'Address:';
const minutesString = 'Minutes';
const outOf5 = '/5';

// Sort
const selectSortOptions = 'Select Sort options:';
const selectSortType = 'Select Sort type:';
const selectSortParameter = 'Select Sort parameter:';

export { customSortOptions, sortTypes, tags, selectPrice, filterRestaurants, restaurantsPerPage, footerNote, minutesString, searchString, noDataFound, outOf5, tagFilters, avgBillFor2, tagsString, selectSortOptions, selectSortType, selectSortParameter, estTimeString, ratingsString, addressString };